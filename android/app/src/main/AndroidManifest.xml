<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    
    <!-- Camera feature -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartFarming"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        
        <!-- Splash Screen Activity -->
        <activity
            android:name=".activities.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.SmartFarming.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- Login Activity -->
        <activity
            android:name=".activities.LoginActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />
        
        <!-- Register Activity -->
        <activity
            android:name=".activities.RegisterActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />
        
        <!-- Main Activity with Bottom Navigation -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="false" />
        
        <!-- Disease Detection Activity -->
        <activity
            android:name=".activities.DiseaseDetectionActivity"
            android:exported="false" />
        
        <!-- Soil Analysis Activity -->
        <activity
            android:name=".activities.SoilAnalysisActivity"
            android:exported="false" />
        
        <!-- Crop Recommendation Activity -->
        <activity
            android:name=".activities.CropRecommendationActivity"
            android:exported="false" />
        
        <!-- Fertilizer Recommendation Activity -->
        <activity
            android:name=".activities.FertilizerRecommendationActivity"
            android:exported="false" />
        
        <!-- Smart Advisory Activity -->
        <activity
            android:name=".activities.SmartAdvisoryActivity"
            android:exported="false" />
        
        <!-- Market Prices Activity -->
        <activity
            android:name=".activities.MarketPricesActivity"
            android:exported="false" />
        
        <!-- Income Comparison Activity -->
        <activity
            android:name=".activities.IncomeComparisonActivity"
            android:exported="false" />
        
        <!-- Government Schemes Activity -->
        <activity
            android:name=".activities.GovernmentSchemesActivity"
            android:exported="false" />
        
        <!-- Expense Tracker Activity -->
        <activity
            android:name=".activities.ExpenseTrackerActivity"
            android:exported="false" />
        
        <!-- Crop Calendar Activity -->
        <activity
            android:name=".activities.CropCalendarActivity"
            android:exported="false" />
        
        <!-- File Provider for Camera -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        
    </application>

</manifest>
